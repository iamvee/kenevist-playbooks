db_pass: "123456789"

git_repo_src: "https://github.com/iamvee/frigg.git"
git_repo_dest: "/root/repos/frigg"
template_path: "{{ git_repo_dest }}/gitlab"
template_file: "docker-compose.yml.j2"

templated_file_dest: "/root/build/docker-compose.yml"
templated_file_owner: root
templated_file_group: root
templated_file_mode: 0600


gitlab_defaults:
  image:
    name: "sameersbn/gitlab"
    tag: "12.4.0"
    
  ports:
    http: 10080
    ssh: 10022

  host: "git.kenevist.ir"

  db:
    name: "name" 
    username: "gitlab"
    password: "password"

  email:
    email: "notifications@example.com"
    reply_to: "noreply@example.com"
    incoming: "reply@example.com"

